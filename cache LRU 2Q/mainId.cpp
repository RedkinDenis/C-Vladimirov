#include "ideal_cache.hpp"
#include <iostream>
// #include "time.h"

using namespace std;

int main() {

    // clock_t start = clock();
    cout << count_hits_Ideal_cache() << endl;
    // clock_t end = clock();

    // cout << (end - start) / CLOCKS_PER_SEC << endl;
}